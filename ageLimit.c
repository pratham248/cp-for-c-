#include <stdio.h>

int main (){

    int x, y, a;
    int t;

    for(int i=0; i<a; i++){
        scanf("%d %d %d", &x, &y, &a); 
        if (x <= a && a < y)
            printf("YES");
        else 
            printf("NO");
        
    }
}